import { ViewStyle } from 'react-native';
interface Props {
    title?: string;
    subTitle?: string;
    onClick?: any;
    style?: ViewStyle;
}
declare const OKeyButton: (props: Props) => JSX.Element;
export default OKeyButton;
