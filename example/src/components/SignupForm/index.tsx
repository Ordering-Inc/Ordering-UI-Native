import React from 'react';
import { View } from 'react-native';
import { OText } from '../shared';

export const SignupForm = (props: any) => {
  return (
    <View>
      <OText size={26}>Sign up form</OText>
    </View>
  );
};
